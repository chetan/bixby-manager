
<h3>Resources for <%= host.name() %></h3>

<p>
  <!-- TODO: this is duped at bottom of page. only show when that is not visible -->
  <button type="button" class="btn btn-primary add_resource_link" host_id="<%= host.get('id') %>">Add Check</button>
  <button type="button" class="btn btn-primary add_trigger_link" host_id="<%= host.get('id') %>">Add Trigger</button>
</p>

<% if (checks.length == 0) { %>

  <p>No checks have been configured</p>

<%
} else {
  __out__ += each_partial(checks, "check", _bvm.Check)
}
%>

<br />
<p>
  <button class="btn btn-primary add_resource_link" host_id="<%= host.get('id') %>">Add Check</button>
  <button class="btn btn-primary add_trigger_link" host_id="<%= host.get('id') %>">Add Trigger</button>
</p>
